
run 

``` bash init ```

It will clone the jdk repos into a tmp folder.

Run `bash diff2 NEWER_TAG OLDER_TAG `  to check for changes between 2 tags.

Run `bash compute-release-diffs`  to run on all tags.
